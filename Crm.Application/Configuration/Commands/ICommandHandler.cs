using MediatR;

namespace Crm.Application.Configuration.Commands
{
    //public interface IRequestHandler<in TCommand> : IRequestHandler<TCommand> where TCommand : ICommand
    //{

    //}

    //public interface IRequestHandler<in TCommand, TResult> : IRequestHandler<TCommand, TResult> where TCommand : ICommand<TResult>
    //{

    //}
}